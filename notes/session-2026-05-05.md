# Session 2026-05-05: Chunk-comparison reconnaissance

## Goal
Q asked me to observe the new chunked comparison workflow, then pick a section to fix using Chrome to visually inspect.

## Observations

- `tests/chunk-results.json` is current (regenerated today, 6152 lines, 266 matched sections).
- Hugo not running at start; started in background as task `b64u95vez`.
- Recent master commits 07a4791..896811e are corporate feature-card tuning.
- Workstream lives in `notes/parity-workstream.md` and `notes/approach-rethink-2026-04-08.md`. Component-fix scout work has been mostly applied.

## Worst chunks right now (top of list)
- `corporate-campuses` desktop+mobile: 5 feature-card sections each at **100%** ("Inclusivity at Its Core", "Real-Time Adaptability", "Seamless Integration", "Beyond Compliance", "Branding and Reputation"). 100% strongly suggests structural mismatch — Hugo likely renders these as a single grid section while WP wraps each card as its own UAGB block, so they count as missing.
- `home` desktop+mobile: 2 unnamed sections at 100% each.
- `universities`: unnamed 100% on both viewports + "Audiom: The Inclusive Digital Map Solution" mobile 100%.
- `about` desktop: "Meet Our Team" 98.5%, an unnamed section 95.7%.
- `about` mobile: unnamed 92.6%, "Meet Our Team" 92.6%.
- `map-evaluation-tool`: "Introduction" 94.4% mobile / 80.6% desktop.
- `blog`: "Navigating the Future..." 78.8%/85.7% and "Latest Blog Posts" 71.6%/74.6%.

## Hypothesis (not yet verified by Chrome)
The five `corporate-campuses` cards at 100% on BOTH viewports look like classifier-level "no match" rather than visual diff — i.e. Hugo emits the cards but the chunker/matcher cannot align them with WP's per-card UAGB containers. Same shape for the unnamed `home`/`universities` 100% entries. Need to load each in Chrome and look at actual DOM to confirm before fixing CSS.

## Decision point
- OPTION A: Fix `corporate-campuses` feature cards — high count of 100% sections, but if root cause is matcher fault not CSS, the fix is in the matcher/chunker, not theme.
- OPTION B: Fix `about` "Meet Our Team" desktop (98.5%) — single section, well-defined heading, matched (so it IS a real visual diff, not a matcher miss).
- OPTION C: Fix `map-evaluation-tool` "Introduction" mobile 94.4% — high diff and standard-single template, broad payoff.

Leaning toward OPTION B for first attack — clean signal, recently touched, and notes already document team gap-rule history. Will confirm with Chrome before touching anything.

## Current blocker
Hugo just starting; need it ready before navigating.

## Update: about/Meet Our Team is a measurement bug, not a CSS bug

**Loaded both pages in Chrome.** Inspected the section:
- WP container: `display:flex, gap:20px, bg:rgb(21,25,29), padding:100px 10px 50px`
- Hugo container: `display:block, gap:normal, bg:rgb(21,25,29), padding:100px 10px 50px` (same bg/padding, different display/gap — but section only has one child, so flex vs block doesn't visibly matter)
- Both H2: color rgb(249,250,251), weight 800, Montserrat, text-align center
- Hugo H2 fontSize 40px vs WP 36.48px (small)
- Inner-blocks-wrap: identical flex/column/center on both

**Read the actual chunk PNGs** (`tests/chunks/about/{wp,hugo}-desktop-section-7.png`):
- Both 1920×~200, dark bg, "Meet Our Team" centered, white text — visually they look IDENTICAL.
- Pixel sample at row 100: both `[21,25,29,255]` — bg pixels match exactly.
- Yet `diffPercent` reports 98.45%, `overlapDiffPercent` 98.44%.

**This looks like a pixelmatch run on misaligned images.** WP=201px tall, Hugo=199px tall. The cropData function in `diffScreenshots` crops both to min(width)×min(height) = 1920×199. But if WP has 1px of top padding extra and the crop cuts WP from y=0..198 vs Hugo y=0..198, the text bands wouldn't align vertically and antialiased text edges would differ everywhere.

Actually wait — the bg sample at y=100 was identical bg (21,25,29) in both. So most of the image IS background. The text occupies maybe 50px tall. If text is offset by 2-3 px vertically between the two images, that's ~5% of pixels disagree at most, not 98%.

**More likely cause:** the chunk screenshot for one of the two captures the FULL inside (text visible) and the other captures *something else entirely* — maybe Hugo's `.uagb-block-7fd0a09e` selector picked up a different element. But I read the PNGs and visually they look the same.

**This is now a measurement-tooling investigation, not a CSS fix.** Picking the wrong battle. Switching target.

## New decision

The 100% scores on `corporate-campuses` feature cards and `home`/`universities` blank-headed sections are also suspicious — those values pin to 100% which is literally the "no overlap area / unmatched" sentinel in `diffScreenshots`. Reviewing the source:

```
if (overlapArea === 0) { diffPercent: 100, overlapDiffPercent: 100, ... }
```

So 100% = "I gave up, no usable overlap." Those are not real diffs either.

**Real fixable diffs (matched, 1920 width, sub-100%):**
- `map-evaluation-tool` Introduction desktop 80.6% (full pixel diff, real)
- `blog` "Latest Blog Posts" desktop 71.6%, mobile 74.6%
- `blog` "Navigating the Future..." desktop 78.8%, mobile 85.7%
- `home` "CONTACT US" mobile 70.4%
- about Section 12 unnamed 52.9%, Section 13 unnamed 95.7% (worth investigating but heading=null is a matcher edge case)

**Picking:** `blog` index page "Latest Blog Posts" desktop 71.6% — it's the blog post grid, has known root-cause documented (background-image cards), and recent commit log claims it was fixed but data still shows high diff. Worth verifying with eyes.

## Tooling promoted

Q noted the `node -e "..."` introspection one-liners should be reusable. Wrote `scripts/chunk-query.js`:
- `worst [N]` / `worst-all [N]` — top N worst sections (filters sentinel-100% by default)
- `page <slug> [viewport]` — every section for one page
- `find <substring>` — search by heading text
- `families` — bucket by page slug, sorted by avg diff
- `stats` — overall counts/mean/median/under-N% buckets

Verified `stats` and `worst 15` work cleanly. From `stats`: 266 total, 17 sentinel (100%), 249 measured, mean 22.79%, median 15.44%, 81 sections under 5%.

## Blog "Latest Blog Posts" fix — applied and verified

**Diagnosis** (from chrome inspection of WP per-block CSS for `uagb-block-a327397c` and `uagb-block-933111fa`):
- WP heading block: `text-align: center`, h2 `font-weight: 700; font-size: 2.5em`
- WP inner-blocks-wrap on alignfull root: `flex-flow: column; align-items: center; justify-content: center; gap: 20px`
- WP section: `padding: 100px 0; gap: 20px`

**Hugo state before:** padding 100px 0 ✓, but missing flex centering on inner-wrap, missing text-align:center on heading block, h2 inheriting global `h2.uagb-heading-text { font-size: 32px; font-weight: 600 }`. So heading rendered LEFT-aligned, smaller, lighter.

**Fix:** Inline-styled the three nested elements in `themes/xrnav/layouts/blog/list.html` to mirror WP's per-block CSS exactly.

**Verified `COMPARE_SLUGS=blog` chunk run:**
- desktop "Latest Blog Posts": 71.6% → **4.5%** (−67pp)
- mobile  "Latest Blog Posts": 74.6% → **23.0%** (−51pp; remaining is mobile-wide font-size/line-height/margins, systemic)
- bonus: hero "Navigating the Future" also fell — desktop 78.8% → 16.3%, mobile 85.7% → 24.7% (the section sits above and benefits from same parent layout)

**Remaining mobile residual on this section** (from style diff output):
- container.font-size 15→16px (Astra mobile default vs browser default)
- container.line-height 23→26px (same)
- container.margin -20→0 (negative margins on .alignfull mobile)
- innerWrapper.max-width min(100%,767px) → min(100%,1200px) — Hugo doesn't shrink wrap on mobile
- heading.font-size 36→40px (em base difference)
These are systemic; not part of this fix.

## Status
Single template edit, no CSS changes. Hugo running on :1314 (background task `b64u95vez`). Ready to commit if Q wants, or chase more sections.

## Committed: 5673b2d (blog template + chunk-query script)

## Systemic mobile work — html font-size and body inheritance

**Problem:** chunk diff showed `container.font-size: 15px → 16px`, `heading.font-size: 36px → 40px` on mobile across many sections. Earlier author note said Astra's `html { font-size: 91.2% }` mobile rule was intentionally omitted because it caused embed-page regressions via rem-based footer spacing.

**Investigation via `scripts/probe-mobile.js` (new helper that opens both pages at 375×812 and prints computed values):**
- WP: html=14.592px, body=14.592px, section=14.592px, h2=36.48px ✓
- Hugo before: html=16px, body=16px, section=16px, h2=40px ✗

**Step 1: re-add `@media (max-width: 921px) { html { font-size: 91.2% } }`** (wordpress-compat.css ~line 2247).
- After: Hugo html=14.592px ✓, but body still 16px, section still 16px.

**Step 2: track down body override.** Found `body.ast-hfb-header { font-size: 16px; line-height: 25.6px; }` at line 144 — hard-coded px instead of inheriting. WP uses `font-size: 1rem; line-height: var(--ast-body-line-height, 1.6em)`.

**Fix:** changed body rule to `font-size: 1rem; line-height: 1.6em`.

**After both:** Hugo html/body/section/h2 all match WP at mobile exactly:
- html=14.592px, body=14.592px, section=14.592px, h2=36.48px

## Result so far

`COMPARE_SLUGS=blog` chunk run:
- desktop "Latest Blog Posts" 4.5% (unchanged from previous)
- mobile  "Latest Blog Posts" 23.0% → **22.4%** (small drop)

Font/sizing inheritance is now correct, but mobile diff barely moved on this section. The remaining ~22% comes from two things still showing in the style diff:
1. `container.margin-left/right: 0 → -20px` (Hugo's mobile rule at line ~1106 adds negative margins to escape entry-content padding; WP doesn't need it)
2. `container.display: flex → block` (Hugo container is `display: block`, WP is flex)
3. `innerWrapper.max-width: min(100%,767px) → min(100%,1200px)` (Hugo wrap stays 1200, WP shrinks)

Need to investigate why entry-content needs the -20px hack on Hugo when WP doesn't, since removing it naively would break full-bleed.

## Pending: regression check on embed pages

Original comment warned 91.2% rule "causes regressions on already-tuned embed pages." Have not yet tested an audiom-embed page. Need to do that before committing the CSS changes.

## Current blocker
Need to verify embed pages haven't regressed. About to run `COMPARE_SLUGS=audiom-demo` (a representative embed) to check.

## Embed regression check passed

Tested 3 embed pages (`audiom-airbus-lopa-a320-200-seat-map`, `events`, `magicmap-paloalto`):
- All 6 page/viewport diffs match the run-103968 baseline to 2 decimal places.
- No regression from html `font-size: 91.2%` or body `font-size: 1rem` changes.
- These pages mostly use heading-split / whole-page selectors and have hardcoded px text, so the rem/em shift is invisible.

## Tooling pitfall to remember

`COMPARE_SLUGS=blog ... playwright test` overwrites `tests/chunk-results.json` with ONLY the filtered subset. Per-page JSONs survive in `tests/.chunk-results/run-*/` and can be used to reconstruct or compare against an old baseline. Currently chunk-results.json holds only the 4 pages I last filtered on (blog + 3 embed). Full run kicked off in background as task `b38w01jp6` to restore the 180-page baseline.

## Now investigating: negative -20px margin on .alignfull mobile

Rule at `wordpress-compat.css:1106` adds `margin-left/right: -20px; width: calc(100% + 40px)` to `.entry-content > .wp-block-uagb-container[style*="padding: 100px"]` at mobile. WP shows 0 margin — it achieves full-bleed differently. Need to check WP's entry-content mobile padding to understand the full-bleed mechanism.

## Root cause found: Hugo's `.ast-container` mobile padding is wrong

`scripts/probe-mobile.js` (extended to dump the full ancestor chain) shows:

| element | Hugo @375 | WP @375 |
|---|---|---|
| `.ast-container` | w=375, padding=`0px 20px` | w=375, padding=`0px` |
| `.content-area` | w=335, x=20 | w=375, x=0 |
| `article` / `.entry-content` | w=335, x=20 | w=375, x=0 |
| `.alignfull` section | w=375 (escapes via -20px margin) | w=375 (no escape needed) |
| `.entry-content > p` | (probably 335 wide) | **w=375, x=0, padding=0** |

WP applies zero side padding on `.ast-container` at mobile. Even regular `<p>` text spans the full 375px viewport with no gutter. Hugo's `main.css:300` rule:

```css
.ast-container {
  padding-left: 20px;
  padding-right: 20px;
}
```

…was added "to match WP" but is the opposite of what WP actually does. The `-20px` margin escape on `.alignfull` was a downstream patch to compensate.

## Plan
1. Remove the 20px `.ast-container` mobile padding rule (`main.css:300-309`)
2. Remove the negative-margin escape rule on alignfull (`wordpress-compat.css:1106-1119` — at minimum the -20px part)
3. Re-run chunks to verify

Side effect of WP-matching: regular text on mobile will touch the screen edge (matches WP's actual behavior; can be revisited as a deliberate UX choice if Q wants).

## Background full chunk run

Task `b38w01jp6` started before I dug into the margin issue. Still running (workers=2, 90 pages × 2 viewports). Will check on completion.

## Current blocker
None — going to apply the two CSS removals and re-test blog.

## Run b38w01jp6 progress

131/180 ok, 9 timeout failures (mostly home/about/corporate-campuses on first attempt — playwright's headless chromium may be hitting parallelism limits at workers=2). 33 node processes still active. ETA: another ~3-4 minutes for tail end.

Plan once it finishes:
1. `node scripts/chunk-query.js stats` for new baseline numbers
2. Remove `.ast-container` 20px mobile padding (main.css:300-303 + .home-page/.audiom-embed-body overrides at 305-309)
3. Remove negative-margin escape on `.entry-content > .wp-block-uagb-container[style*="padding: 100px"]` (wordpress-compat.css:1106-1119, margin-left/right -20px and width calc bits)
4. Re-run targeted COMPARE_SLUGS=blog,about,privacy-policy,home,corporate-campuses
5. If healthy, commit
6. Continue with `node scripts/chunk-query.js worst 20` against new baseline

## Baseline established + first attempt PARTIAL REGRESSION

Run b38w01jp6 finished: 166 ok, 14 timeouts (heavy pages, separate playwright concurrency issue).
- mean diff **14.48%** / median **9.22%** (from 22.79%/15.44% in pre-fix data) — solid wins from html font-size + body 1rem changes alone.

Then I removed `.ast-container` 20px mobile padding and the -20px escape on `.entry-content > .wp-block-uagb-container[style*="padding: 100px"]` and `.blog-hero`. Targeted re-run shows:
- blog mobile: 38.11% → 37.28% (−0.83) ✓
- privacy-policy mobile: 26.55% → 26.6% (~flat) ✓
- corporate-campuses desktop: 10.88% → 10.88% (flat) ✓
- **corporate-campuses mobile: 16.92% → 94.40% (+77.48 REGRESSION)** ✗

## Root cause of corporate-campuses regression

Many OTHER mobile escape rules still reference `width: calc(100% + 40px); margin-left: -20px; margin-right: -20px` — they were also written assuming .ast-container has 20px padding. With padding gone, they now push content NEGATIVELY past viewport edge. Found in:
- `.collection-page .info-box-section` (line 2883-2886) — fixed
- `.collection-page--health-care-facilities > .info-box-section:nth-of-type(2)` (3013-3016)
- `.collection-page--corporate-campuses .feature-cards` (3033-3035)
- `.collection-page .collection-hero` (3075-3078)
- `.collection-page .collection-cta` (3096-3099)
- `.wp-accessibility-page` (6776+)

Removing all of these systematically.

## Current blocker
Need to finish bulk removal of the calc/-20px escape rules, then re-test.

## Bulk removal done — corp-campuses mobile recovered, desktop NOW broken

Removed all 6 escape blocks (info-box, health-care info-box, corp feature-cards, collection-hero, collection-cta, wp-accessibility-page) — `grep` confirms no `margin-(left|right): -20px` or `width: calc(100% + 40px)` remains.

Targeted re-run shows:
- corporate-campuses mobile: 94.40% → **16.92% (restored)** ✓
- corporate-campuses **desktop: 10.88% → 66.20% (+55pp NEW regression)** ✗
- health-care-facilities mobile: 30.52% → 28.54% ✓
- universities mobile: 40.61% → 40.13% ✓
- privacy-policy/blog/health-care desktop: flat ✓

## Investigating corporate-campuses desktop regression

Desktop sections show 100% on the 5 feature-card chunks ("Inclusivity at Its Core", "Real-Time Adaptability", "Seamless Integration", "Beyond Compliance", "Branding and Reputation"). In baseline these were 6-10% real diffs, not sentinels. So either:
- Screenshots returning width/height 0 (sentinel path)
- Layout shift causing pixelmatch to return 100%

My CSS edits were all inside `@media (max-width: 921px)` so desktop CSS should be unchanged. Need to inspect the JSON dimensions to find which path is failing.

## Resolved: 100% sentinels were screenshot-loop test timeouts, not CSS regressions

Per-section JSONs show wpWidth=0, hugoWidth=0 → screenshot capture failed mid-loop after the 30s test timeout fired. Re-running corp-campuses solo with workers=1 produced 10.88%/16.92% — exactly matching baseline. So the CSS removals are non-regressing.

## Wins from full ast-container removal

Targeted 10-page sweep (workers=1, baseline run-72212 → current):
| page | desktop | mobile |
|---|---|---|
| blog | 22.12% (=) | 38.11→**37.28%** (−0.83) |
| brandon-keith-biggs | 25.65% (=) | 32.72% (=) |
| corporate-campuses | 10.88% (=) | 16.92% (=, after flake retry) |
| covid-statistic-text-map | 0.63% (=) | 41.35→**26.31%** (−15.04) |
| gallery | 26.89% (=) | 28.73→**22.70%** (−6.03) |
| health-care-facilities | 15.30% (=) | 30.52→**28.54%** (−1.98) |
| implementation | 28.35% (=) | 28.61→**20.11%** (−8.50) |
| map-evaluation-tool | 80.61% (=) | 87.20% (=) — different cause |
| privacy-policy | 4.44% (=) | (not run, prior 26.6%≈baseline) |
| universities | 12.19% (=) | 40.61→**40.13%** (−0.48) |

Net: desktop unchanged (expected — all edits were `@media (max-width: 921px)`), mobile broadly down 1–15pp on the affected pages. Map-evaluation-tool still 80%/87% — separate fix needed (heading sizing & section padding).

Ready to commit.
